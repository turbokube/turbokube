{
  "name": "turbokube-examples",
  "scripts": {
    "build:images": "cd ../ && TEST=false ./build.sh",
    "build": "yarn run build:images",
    "test:images": "cd ../ && BUILD=false ./build.sh",
    "test:integration": "vitest run --single-thread",
    "test": "yarn run test:images && yarn run test:integration"
  },
  "devDependencies": {
    "@turbokube/spawnwait": "workspace:*",
    "vitest": "0.31.1"
  }
}
