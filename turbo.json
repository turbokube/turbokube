{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
    },
    "test": {
      "dependsOn": [
        "build"
      ]
    },
    // TODO set up pipeline to rebuild images only when necessary
    "docker:load": {
      "inputs": [
        // we probably need an imagesource convention because we might do COPY
        "../Dockerfile",
        "../.dockerignore"
      ],
      "outputs": [
      ]
    }
  }
}
