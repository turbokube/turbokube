{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": [
        "package.json",
        "../Dockerfile",
        "../.dockerignore"
      ]
    },
    "test": {
      "dependsOn": [
        "build"
      ]
    }
  }
}
